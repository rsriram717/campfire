{
  "version": 2,
  "buildCommand": "pip install -r requirements.txt && echo '>>> Dependencies installed' && PYTHONPATH=. FLASK_ENV=production flask db upgrade && echo '>>> Migrations completed'",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "app.py"
    }
  ],
  "framework": "python"
} 