# Ranking Overhaul

Two-stage pipeline: score_candidates() + mmr_select() deterministically picks 3 restaurants, then Haiku explains only. Removes dine-in/reservable signals throughout.

See plan in conversation history for full details.
